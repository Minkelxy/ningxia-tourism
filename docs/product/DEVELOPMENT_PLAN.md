# 开发计划

> 本文档记录项目开发阶段、已完成目标与后续规划，用于管理和追踪开发进度。

---

## 当前版本：v0.3.100（2026-09-02）

#### v0.3.100 — 路线时间线绘图与动效衔接完善（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 时间线绘图 | 已完成 | 停靠点之间增加渐进绘制的路线轨道，与现有时间线和品牌色保持一致 |
| 停靠点入场 | 已完成 | 停靠点按阅读顺序错峰入场，时段细节继续沿用同一节奏 |
| 响应式与可访问性 | 已完成 | 320px 窄屏调整轨道位置，`prefers-reduced-motion` 下恢复静态轨道与内容 |
| 回归覆盖 | 已完成 | 双端 E2E 检查路线轨道、错峰延迟和减少动效回退 |

#### v0.3.99 — 地图视口动效与拖拽衔接完善（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 视口过渡 | 已完成 | 缩放、重置和城市/区县层级切换使用平滑变换过渡，地图绘图层级变化更连贯 |
| 拖拽体验 | 已完成 | 拖拽期间关闭过渡保持即时跟手，结束后恢复平滑状态 |
| 动效可访问性 | 已完成 | `prefers-reduced-motion` 下关闭视口过渡，保留静态地图交互 |
| 回归覆盖 | 已完成 | 双端 E2E 与视口 hook 测试覆盖动效状态和拖拽状态切换 |

#### v0.3.98 — 山河绘图与地图入场动效完善（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页绘图 | 已完成 | 太阳、河流与轨道 CSS 图形增加低幅度呼吸、流动和漂移动效，保持现有配色与构图 |
| 地图绘图 | 已完成 | 地图区域按顺序进行描边绘制入场，选中景点使用轻量脉冲提示，不改变地图交互逻辑 |
| 动效可访问性 | 已完成 | `prefers-reduced-motion` 下关闭新增动效并恢复静态绘图 |
| 回归覆盖 | 已完成 | 双端 E2E 检查首页绘图、地图区域动效、绘制顺序和减少动效设置 |

#### v0.3.97 — 列表筛选输入触控节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 筛选输入 | 已完成 | 景点与美食筛选面板的搜索输入本体补齐为至少 44px 高，与下拉控件和全站主要操作共享触控基线 |
| 筛选行为 | 已完成 | 保留桌面横向布局、移动端折叠与既有查询参数同步，不改变筛选逻辑 |
| 回归覆盖 | 已完成 | E2E 检查筛选输入和下拉控件在桌面/移动端均满足统一的实际触控高度 |

#### v0.3.96 — 响应式站点壳层高度统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 壳层高度 | 已完成 | 首页、全屏状态页和离线提示统一引用 `--site-header-height`，消除固定值与移动头部高度的偏差 |
| 状态页体验 | 已完成 | 空状态在桌面/移动端跟随实际站点头部高度，保留原有内容与视觉层级 |
| 回归覆盖 | 已完成 | 桌面/移动双端 E2E 检查状态页高度与响应式头部变量一致 |

#### v0.3.95 — 搜索输入触控节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 搜索输入 | 已完成 | 全站搜索输入控件本体补齐为至少 44px 高，与筛选和手记搜索输入共享触控基线 |
| 搜索行为 | 已完成 | 保留桌面端自动聚焦、移动端不自动唤起键盘、清空和提交行为 |
| 回归覆盖 | 已完成 | 桌面/移动双端 E2E 检查输入最小高度与既有搜索入口反馈 |

#### v0.3.94 — 窄屏地图控制区节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 地图操作布局 | 已完成 | 320px—480px 下统一为三列两行，图层与缩放/重置操作不再随宽度产生不同换行 |
| 触控与边界 | 已完成 | 保留 44px 以上控制热区，窄屏页面根宽度不溢出 |
| 回归覆盖 | 已完成 | 移动端 E2E 检查六个控制项、三列两行布局与根页面宽度 |

#### v0.3.93 — 收藏列表中窄屏节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 中窄屏内容节奏 | 已完成 | 320px—480px 下标题/副标题单行省略，路线摘要隐藏，避免 390px 路线条目异常变高 |
| 操作层级 | 已完成 | 320px—360px 使用 44×44 图标按钮，390px 保留文字按钮并保持 44px 高度 |
| 回归覆盖 | 已完成 | 桌面/移动双端同一用例覆盖 320px 与 390px，验证行高、按钮尺寸和页面宽度 |

#### v0.3.92 — 收藏列表极窄屏节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 收藏列表操作 | 已完成 | 320px—360px 下收藏按钮图标化为 44×44 热区，避免按钮文字挤压条目 |
| 内容节奏 | 已完成 | 极窄屏单行省略景点标题/副标题，隐藏路线摘要，景点与路线条目保持紧凑行高 |
| 可访问性与回归 | 已完成 | 保留 aria-label/title，桌面/移动双端以已填充收藏状态验证行高、热区和页面宽度 |

#### v0.3.91 — 景点卡片极窄屏操作区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 景点卡片操作区 | 已完成 | 320px—768px 下“加入对比”和“查看出行信息”保持单行，与美食、路线卡片统一行动节奏 |
| 触控与宽度 | 已完成 | 保留 44px 触控高度，操作入口不突破卡片内边距，页面不横向溢出 |
| 回归覆盖 | 已完成 | 桌面/移动双端验证默认与“已加入对比”状态的操作区布局 |

#### v0.3.90 — 行前指南首屏操作区收口（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 行前指南操作区 | 已完成 | 两个主要入口在 320px、360px 下等宽同排，避免首屏操作断行 |
| 跨页面规则 | 已完成 | 与路线详情操作区共享极窄屏 6px 间距和收口策略 |
| 回归覆盖 | 已完成 | 浏览器回归检查指南与路线详情入口同排、触控高度和页面宽度 |

#### v0.3.89 — 极窄屏首屏操作区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 行前指南操作区 | 已完成 | “按季节开始”和“直接看路线”在 320px、360px 下等宽同排 |
| 跨页面规则 | 已完成 | 与路线详情操作区共享极窄屏 6px 间距和收口策略 |
| 回归覆盖 | 已完成 | 浏览器回归检查指南与路线详情入口同排、触控高度和页面宽度 |

#### v0.3.88 — 极窄屏路线操作区保持完整（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 路线详情操作区 | 已完成 | 打印、分享、收藏三个入口在 320px、360px 下保持同一行 |
| 操作区层级 | 已完成 | 保留 390px 与桌面端原有按钮尺寸、颜色和触控高度 |
| 回归覆盖 | 已完成 | 浏览器回归检查入口同排、44px 以上触控高度和页面宽度约束 |

#### v0.3.87 — 路线详情极窄屏操作区收口（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 路线详情操作区 | 已完成 | 打印、分享、收藏三个入口在 320px、360px 下保持同一行 |
| 窄屏间距 | 已完成 | 仅在极窄断点收紧按钮间距和横向内边距，保留 390px 及桌面端原有层级 |
| 回归覆盖 | 已完成 | 浏览器回归检查三枚入口同排、触控高度和页面宽度约束 |

#### v0.3.86 — 关联导航箭头反馈统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 详情关联入口 | 已完成 | “周边推荐”箭头与资料来源入口统一使用胡杨绿和轻量右移反馈 |
| 搜索结果入口 | 已完成 | 搜索结果末端箭头补齐同一套颜色与悬停反馈 |
| 回归覆盖 | 已完成 | 桌面、移动浏览器回归覆盖详情关联与搜索结果两类入口 |

#### v0.3.85 — 城市详情关联卡片响应式收口（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 城市详情关联卡片 | 已完成 | 移动端图片宽度与 100px 内容列统一，避免卡片内裁切并保持统一视觉比例 |
| 窄屏适配 | 已完成 | 覆盖 320px 页面宽度约束，城市详情关联卡片不撑宽页面 |
| 回归覆盖 | 已完成 | 增加城市详情关联图片与内容列宽一致性 E2E，并复核五城概览卡片 |

#### v0.3.84 — 城市概览卡片节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 五城概览卡片 | 已完成 | 内容区使用纵向弹性布局，桌面双列每行的城市指南入口保持底部对齐 |
| 移动端适配 | 已完成 | 保留城市图片按卡片宽度自适应与 16:9 比例，页面根宽度不溢出 |
| 回归覆盖 | 已完成 | E2E 检查桌面按行对齐，并覆盖 320px 城市卡片图片与页面宽度 |

#### v0.3.83 — 城市概览卡片节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 五城概览卡片 | 已完成 | 内容区使用纵向弹性布局，桌面双列每行的城市指南入口保持底部对齐 |
| 移动端适配 | 已完成 | 保留城市图片按卡片宽度自适应与 16:9 比例，页面根宽度不溢出 |
| 回归覆盖 | 已完成 | E2E 检查桌面按行对齐，并覆盖 320px 城市卡片图片与页面宽度 |

#### v0.3.82 — 旅行手记卡片节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 旅行手记卡片 | 已完成 | 使用纵向弹性布局，桌面双列每行的“阅读全文”入口保持底部对齐 |
| 移动端布局 | 已完成 | 保留单列内容高度自适应，页面根宽度不被卡片撑开 |
| 回归覆盖 | 已完成 | E2E 检查桌面按行对齐，以及移动端 15 张卡片单列与页面宽度 |

#### v0.3.81 — 首页专题卡片节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页专题卡片 | 已完成 | 使用纵向弹性布局，桌面端三张卡片的“阅读全文”入口保持底部对齐 |
| 全站卡片节奏 | 已完成 | 首页专题与景点、美食、推荐路线卡片共享“内容填充、行动收口”规则 |
| 响应式与回归 | 已完成 | 保留中屏组合卡片与移动端单列布局，并增加首页桌面端对齐检查 |

#### v0.3.80 — 内容卡片节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 景点与美食卡片 | 已完成 | 使用纵向弹性布局，行动入口在桌面网格中保持底部对齐 |
| 全站卡片节奏 | 已完成 | 与推荐路线卡片共享“内容填充、行动收口”的布局规则 |
| 回归覆盖 | 已完成 | E2E 检查桌面端景点与美食首行卡片对齐，并覆盖移动端跳过逻辑 |

#### v0.3.79 — 推荐路线卡片节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 卡片布局 | 已完成 | 推荐路线卡片使用纵向弹性布局，内容长度变化时行动入口仍保持底部对齐 |
| 移动适配 | 已完成 | 保留移动端单列、原有图片比例与信息密度 |
| 回归覆盖 | 已完成 | E2E 检查桌面端前两张卡片行动入口底部对齐，并覆盖语义分组 |

#### v0.3.78 — 卡片与媒体选择语义统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 路线卡片概览 | 已完成 | 内容核实信息补充带名称的 `role="group"` 语义分组 |
| 景点详情媒体 | 已完成 | 多图选择器补充带名称的 `role="group"` 语义分组 |
| 回归覆盖 | 已完成 | E2E 检查路线卡片语义分组，并覆盖桌面与移动端 |

#### v0.3.77 — 进度与主题图形语义统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页主题图形 | 已完成 | 山河主题图形补充图像角色与可访问名称 |
| 行前清单进度 | 已完成 | 使用标准进度条语义实时暴露已完成数量与总数 |
| 回归覆盖 | 已完成 | E2E 检查首页主题图形语义与清单勾选后的进度更新 |

#### v0.3.76 — 五城卡片窄屏图片比例收口（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 城市卡片图片 | 已完成 | 320/390px 下按卡片宽度自适应，解除宽高比与最小高度叠加导致的超宽 |
| 视觉比例 | 已完成 | 窄屏继续保持 16:9 图片比例与原有裁切风格 |
| 回归覆盖 | 已完成 | E2E 检查 320px 城市卡片图片、卡片宽度与页面宽度 |

#### v0.3.75 — 地图控制语义与操作反馈统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 地图控制分组 | 已完成 | 地图层级面包屑与地图控制区补充独立的 `role="group"` 与可访问名称 |
| 操作反馈 | 已完成 | 区县图例按钮使用与可点击行为一致的指针光标，保留统一悬停与聚焦反馈 |
| 回归覆盖 | 已完成 | E2E 检查桌面与移动端地图层级、地图控制分组和图例交互 |

#### v0.3.74 — 路线筛选分组语义统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 筛选分组 | 已完成 | 行程天数、涉及城市、旅行主题和行程节奏均补充独立的 `role="group"` 与可访问名称，保持原有布局和筛选行为 |
| 回归覆盖 | 已完成 | E2E 检查路线筛选区域中的四个语义分组，并继续覆盖筛选地址同步与结果更新 |
| 地图图例 | 已完成 | 区县颜色图例支持键盘聚焦与鼠标悬停联动区域高亮，离开后清除临时高亮且不污染地图区域键盘焦点 |
| 回归覆盖 | 已完成 | E2E 覆盖桌面与移动端图例可见性、聚焦高亮和离开后状态清除 |

#### v0.3.73 — 地图懒加载状态视觉统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 占位画布 | 已完成 | 懒加载未触发时沿用正式地图的沙纸渐变、点状纹理与抽象宁夏轮廓，消除纯白空卡 |
| 加载语义 | 已完成 | 保留 `role="status"`、明确加载文案与装饰性加载动画 |
| 回归覆盖 | 已完成 | Playwright 固定 IntersectionObserver 未触发场景，检查占位文案与视觉样式 |

#### v0.3.72 — 地图点位触控范围统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 景点热区 | 已完成 | 景点透明热区统一为 `r=38`，在地图缩放后仍保持至少 44px 的实际屏幕尺寸 |
| 指针命中 | 已完成 | 透明 SVG 热区不直接抢占事件，由地图画布按最近点位分发，避免相邻点位互相遮挡 |
| 回归覆盖 | 已完成 | Playwright 双端检查景点热区实际渲染宽高，美食热区回归保持通过 |

#### v0.3.71 — 路由跳转焦点与内容承接统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 页面承接 | 已完成 | 桌面端路径变化后自动聚焦新的 `main#main-content`，避免焦点留在旧页面链接 |
| 移动端连续性 | 已完成 | 移动菜单跳转继续保留菜单按钮焦点，不打断既有收起与滚动恢复 |
| 回归覆盖 | 已完成 | 单元测试覆盖路径变化与菜单焦点例外，Playwright 覆盖桌面与移动端行为 |

#### v0.3.70 — 地图美食点位触控热区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 触控热区 | 已完成 | 可交互美食点位增加透明热区，手机点选范围与景点点位保持一致，视觉标记不变 |
| 聚焦反馈 | 已完成 | 美食点位获得与景点一致的胡杨绿键盘聚焦高亮 |
| 回归覆盖 | 已完成 | 桌面端与移动端 E2E 覆盖热区属性、Tab 聚焦和地图点位行为 |

#### v0.3.69 — 移动端菜单跳转即时收束（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 跳转反馈 | 已完成 | 点击移动端菜单入口后立即收起悬浮面板，避免新页面被旧菜单短暂遮挡 |
| 焦点连续性 | 已完成 | 路由跳转前将焦点回收到菜单按钮，键盘用户继续拥有稳定的返回位置 |
| 回归覆盖 | 已完成 | Header 组件测试与移动端 Playwright E2E 覆盖关闭、目标路由和焦点恢复 |

#### v0.3.68 — 浅色美食详情操作区样式隔离（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 样式作用域 | 已完成 | 美食详情使用独立操作区 class，不再继承路线深色头图按钮样式 |
| 对比度 | 已完成 | “分享美食”恢复浅色页面正文色、白色半透明背景与统一悬停反馈 |
| 回归覆盖 | 已完成 | 桌面端与移动端 E2E 检查美食详情操作区样式 |

#### v0.3.67 — 路线详情深色操作区视觉统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 操作层级 | 已完成 | 路线详情深色头图中的打印、分享与收藏使用同一套半透明按钮层级 |
| 状态反馈 | 已完成 | 收藏保留 `aria-pressed`、已收藏状态、金色聚焦边框与轻量抬升反馈 |
| 回归覆盖 | 已完成 | 桌面端与移动端 E2E 检查默认、悬停和聚焦样式 |
| 门禁稳定性 | 已完成 | 首页完整筛选入口改用真实 Tab 键聚焦路径，消除 Chromium hover 断言波动 |

#### v0.3.66 — GitHub 内容审计入口修正（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 文档入口 | 已完成 | 关于页内容审计链接指向实际的 `docs/content/CONTENT_AUDIT.md` |
| 可访问性 | 已完成 | 用户可从关于页继续核对来源与复核记录 |
| 回归覆盖 | 已完成 | 桌面与移动端 E2E 均检查 GitHub 文档入口地址 |

#### v0.3.65 — 窄屏手记栏目浏览统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 窄屏浏览 | 已完成 | 320px 等窄屏下手记栏目标签可横向滚动，最后栏目不再被裁切 |
| 交互保持 | 已完成 | 保留圆角标签、当前选中态与键盘左右切换 |
| 回归覆盖 | 已完成 | 桌面与移动项目均以 320px 视口检查最后栏目完整可见 |

#### v0.3.64 — 比较表操作入口触控尺寸统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 入口宽度 | 已完成 | 五城与路线比较表的“查看”入口统一为至少 44px 宽 |
| 对齐方式 | 已完成 | 操作文字与箭头居中，保持表格信息密度与横向滚动行为不变 |
| 回归覆盖 | 已完成 | 桌面与移动端 E2E 均检查比较表操作入口宽度 |

#### v0.3.63 — 路线详情停靠点入口触控热区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 入口尺寸 | 已完成 | 路线详情时间线中的停靠点名称链接统一为至少 44px 高 |
| 时间线节奏 | 已完成 | 保留停靠点时间、核实标签和原有文字悬停反馈，只增加名称入口垂直空间 |
| 回归覆盖 | 已完成 | 桌面与移动端 E2E 均检查路线详情停靠点名称入口 |

#### v0.3.62 — 比较表名称入口触控热区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 入口尺寸 | 已完成 | 城市比较表与路线比较表名称链接统一为至少 44px 高 |
| 信息密度 | 已完成 | 保留表格结构与横向滚动，只增加名称入口的垂直点击空间 |
| 回归覆盖 | 已完成 | 桌面与移动端 E2E 均检查两类比较表名称入口 |

#### v0.3.61 — 内容卡片标题触控热区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 入口尺寸 | 已完成 | 景点、美食与首页旅行专题卡片标题统一为至少 44px 高的可点击入口 |
| 视觉节奏 | 已完成 | 标题垂直留白与既有卡片层级、颜色和悬停反馈保持一致 |
| 回归覆盖 | 已完成 | 桌面与移动端 E2E 均检查三类内容卡片标题入口 |

#### v0.3.60 — 页脚当前页面反馈统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 路径匹配 | 已完成 | 顶部桌面导航、移动菜单与页脚共用 `isNavigationPathActive`，深层内容路由保持一致的选中结果 |
| 当前反馈 | 已完成 | 页脚当前内容入口同步使用金色选中态与 `aria-current="page"` |
| 回归覆盖 | 已完成 | 单元测试与桌面/移动端 E2E 均覆盖页脚当前页面标记 |

#### v0.3.59 — 全站导航入口单一配置（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 配置统一 | 已完成 | 顶部桌面导航、移动菜单和页脚内容入口共用 `src/lib/site-navigation.ts` |
| 入口一致 | 已完成 | 六类内容路径由同一配置渲染，避免新增入口时顶部与页脚发生漂移 |
| 回归覆盖 | 已完成 | E2E 对比顶部与页脚的入口名称及路径，锁定跨端一致性 |

#### v0.3.58 — 页脚导航与主导航入口统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 内容入口 | 已完成 | 页脚“继续探索”补齐宁夏美食，与顶部导航的六类内容入口保持一致 |
| 导航语义 | 已完成 | 页脚内容入口与资料入口分别使用带标签的 `nav` 分组 |
| 回归覆盖 | 已完成 | 增加页脚路径与导航区域查询测试，防止入口遗漏或分组退化 |

#### v0.3.57 — 城市比较表移动端溢出修复（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 横向滚动 | 已完成 | 城市比较表继续在自身容器内横向滚动，不牺牲宽表格的信息密度 |
| 页面宽度 | 已完成 | 通过绘制隔离阻断表格最小宽度向页面根布局传播，320px 页面不再出现 971px 隐藏溢出 |
| 回归覆盖 | 已完成 | 移动端 E2E 同时检查表格内部可滚动性和根页面宽度 |

#### v0.3.56 — 移动导航悬停反馈统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 交互反馈 | 已完成 | 移动端未选中菜单项补齐白底、胡杨绿文字与轻量阴影，与桌面导航保持同一视觉节奏 |
| 行为兼容 | 已完成 | 不改变菜单入场动效、遮罩、焦点循环、滚动锁定、触控热区和当前页语义 |
| 回归覆盖 | 已完成 | E2E 等待菜单入场完成后检查移动菜单项悬停样式，避免动效过程影响验证 |

#### v0.3.55 — 极窄屏品牌栏收口（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 品牌排版 | 已完成 | 360px 以下隐藏重复副标题，320px 宽度下品牌主名保持单行并与右侧入口对齐 |
| 无障碍语义 | 已完成 | 品牌链接保留完整 `aria-label`，视觉收口不改变屏幕阅读器名称 |
| 回归覆盖 | 已完成 | E2E 检查品牌高度、窄屏副标题状态及页面横向宽度 |

#### v0.3.54 — 收藏页移动首屏收口（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首屏节奏 | 已完成 | 移动端收藏页移除遗留的 300px 底部留白，主要内容更早进入可视范围 |
| 桌面兼容 | 已完成 | 桌面端保持 compact hero 原有 58px 底部间距 |
| 回归覆盖 | 已完成 | E2E 同时检查移动端 76px 与桌面端 58px 英雄区底部内边距及内容间距 |

#### v0.3.53 — 公共反馈层安全区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 底部反馈 | 已完成 | 收藏反馈、PWA 更新提示与景点对比停靠条统一使用底部安全区偏移 |
| 跨端间距 | 已完成 | 桌面端保持原有间距，移动端仅在存在系统手势区时追加安全偏移 |
| 回归覆盖 | 已完成 | 桌面端与移动端检查三类公共反馈层的安全区规则 |

#### v0.3.52 — 公共加载态品牌化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 等待态视觉 | 已完成 | 路由懒加载使用与站点品牌标志一致的地图印章、主题色、圆角与阴影语言 |
| 状态语义 | 已完成 | Loading 提供明确的 `role=status` 与“页面加载中”可访问名称，并保留 reduced-motion 兼容 |
| 回归覆盖 | 已完成 | 新增 Loading 组件单元测试，覆盖状态文案、品牌印章容器与可访问语义 |

#### v0.3.51 — 地图预览入场节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 跨端节奏 | 已完成 | 桌面端与移动端地图景点预览统一使用 `map-preview-in` 轻量入场动效 |
| 移动细节 | 已保留 | 移动端继续保留面板把手、安全区与 reduced-motion 兼容行为 |
| 回归覆盖 | 已完成 | E2E 同时覆盖桌面端绝对定位与移动端固定底部面板的动效状态 |

#### v0.3.50 — 移动端地图预览层细节优化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 面板提示 | 已完成 | 移动端景点预览增加面板把手，强化底部面板的边界与关闭提示 |
| 入场节奏 | 已完成 | 增加轻量上滑入场动效，尊重 reduced-motion 设置并保持安全区 |
| 回归覆盖 | 已完成 | 单元测试与移动端 E2E 覆盖固定定位、把手语义和入场动效 |

#### v0.3.49 — 移动端导航遮罩层级优化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 面板可读性 | 已完成 | 移动端导航使用实色纸张面板，避免底层标题或操作内容透出 |
| 菜单层级 | 已完成 | 增加菜单外轻量遮罩与背景虚化，点击遮罩即可关闭菜单 |
| 回归覆盖 | 已完成 | 单元测试与移动端 E2E 覆盖遮罩可见性、点击关闭、滚动锁定和内容不位移 |

#### v0.3.48 — 移动端导航层级与滚动体验优化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 菜单层级 | 已完成 | 移动端导航改为实色悬浮面板，不推移页面首屏，并使用统一纸张背景与阴影 |
| 背景滚动 | 已完成 | 菜单打开时锁定背景滚动，关闭、Escape 和路由切换后恢复原有设置 |
| 回归覆盖 | 已完成 | 单元测试与移动端 E2E 覆盖悬浮定位、层级、滚动锁定和关闭恢复 |

#### v0.3.47 — 路线详情吸顶节奏统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 响应式高度 | 已完成 | 站点头部、路线按天导航、日程锚点和 DAY 标记共享响应式高度变量 |
| 跳转安全区 | 已完成 | `scroll-margin-top` 与 sticky 位置随桌面端、移动端和 375—480px 手机头部高度变化 |
| 回归覆盖 | 已完成 | 路线详情桌面端/移动端回归目标标题位于两层吸顶栏下方 |

#### v0.3.46 — 路线日程深链接定位优化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| Hash 定位 | 已完成 | 路线详情内容加载后识别 `#route-day-N`，定位到对应日程并同步当前天数状态 |
| Sticky 间距 | 已保留 | 继续使用日程区块的 `scroll-margin-top`，避免标题被站点头部和按天导航遮挡 |
| 回归覆盖 | 已完成 | 增加路线日程深链接的桌面端/移动端端到端回归 |

#### v0.3.45 — 查询条件变化播报优化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 查询参数播报 | 已完成 | 路径不变时切换搜索词或列表筛选，路由播报器会重新触发页面标题播报 |
| 浏览位置 | 已保留 | 筛选参数变化不触发滚动回顶，继续保持当前浏览上下文 |
| 回归覆盖 | 已完成 | 增加同一路径搜索参数变化的路由播报单元测试 |

> 本阶段完成合并收口：统一全站视觉语言，补齐网络实拍照片、来源记录与图片参考范围说明，优化搜索框聚焦反馈、搜索清空操作、搜索快捷关键词、比较结果入口、资料目录、PWA 更新提示、地图图层选中态与语义、导航当前页语义、景点兴趣筛选选中态、次级行动按钮、卡片细节、导航/选择控件、筛选面板、次级目的地、阅读入口、说明页目录、详情页操作状态、深色头图按钮对比度、轻量文字操作状态、清除/重置类文字操作、结果区清除筛选、详情返回入口、资料来源入口、首页内容方法入口、导航搜索入口、收藏入口触控热区、首页/搜索起始入口、收藏操作反馈、景点对比操作反馈、旅行手记栏目切换反馈、路线筛选反馈与行前清单反馈，并统一 GitHub Pages Actions 发布源与深层链接回退验收。

#### v0.3.44 — 移动端搜索首屏聚焦优化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 搜索焦点 | 已完成 | 桌面端自动聚焦输入框；移动端保持未聚焦，避免进入页面时自动唤起系统键盘 |
| 交互保持 | 已完成 | 清空后回焦、键盘输入和 URL 同步逻辑不变 |
| 回归覆盖 | 已完成 | E2E 分别覆盖桌面端与移动端搜索输入焦点状态 |

#### v0.3.43 — 页面主内容语义统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 页面地标 | 已完成 | 搜索页、收藏页与开发地图查看器移除嵌套 `<main>`，由应用外壳统一提供唯一主内容区域 |
| 回归覆盖 | 已完成 | 搜索与收藏页面增加单一主内容区域端到端回归 |

#### v0.3.42 — 内容页首屏图片比例统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 移动端首图比例 | 已完成 | 美食、景点、路线及相关内容页首图统一为 4:3 视觉框，竖幅素材使用 `object-fit: cover` 稳定首屏高度 |
| 内容边界 | 已完成 | 保留图片来源、许可记录、alt 文本和实拍/参考图说明 |
| 回归覆盖 | 已完成 | 增加移动端美食、景点、路线首图比例与高度回归 |

#### v0.3.41 — GitHub Pages 深层链接与发布源统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| Pages 发布源 | 已完成 | GitHub Pages 切换为 GitHub Actions，移除 legacy 分支发布竞争 |
| 深层链接回退门禁 | 已完成 | 构建后校验 `dist/404.html` 与 `index.html` 地址恢复逻辑 |
| 线上直达验收 | 已完成 | 首页与 `/attractions` 直接访问均由线上 SPA 正常接管 |

### 已完成的开发阶段

#### v0.1 — 基础框架与核心功能

| 功能 | 状态 | 说明 |
|------|------|------|
| React + TypeScript + Vite 项目搭建 | 已完成 | React 18 / TS 5.8 / Vite 6 |
| Tailwind CSS 样式系统 | 已完成 | 自定义配色（沙金/胡杨绿/枸杞红）与动画 |
| 自研 SVG 地图 | 已完成 | WGS84 投影、GeoJSON 边界、Pointer Events 缩放平移 |
| 景点数据与详情页 | 已完成 | 22 个 published 景点，含来源引用与核实等级 |
| 城市概览页 | 已完成 | 5 个地级市，含旅行角色与行程提醒 |
| 路线推荐与详情页 | 已完成 | 9 条主题路线，逐日时间槽全覆盖 |
| 旅行手记（资料专题） | 已完成 | 15 篇 published，含搜索筛选 |
| 行前指南 | 已完成 | 四季建议、跨城原则、本机清单 |
| 数据校验门禁 | 已完成 | ID 格式、模板电话、类型安全、占位文本 |
| 构建与部署 | 已完成 | GitHub Actions → GitHub Pages |

#### v0.2 — 地图增强与数据完善（7 轮优化）

| 轮次 | 方向 | 状态 | 说明 |
|------|------|------|------|
| 1 | 美食数据转正 | 已完成 | 14 道美食全部 published（2 verified + 12 review） |
| 2 | 路线时间槽补齐 | 已完成 | 9 条路线逐日 timeSlots 全覆盖 |
| 3 | 美食详情页联动 | 已完成 | /foods 列表页 + /food/:id 详情页 + 地图点击跳转 |
| 4 | 图层键盘无障碍 | 已完成 | FoodLayer role=button + onKeyDown；Transport/Government 保持 role=img 纯展示 |
| 5 | verifiedAt 周期校验 | 已完成 | 180 天过期门禁，覆盖 5 类数据 |
| 6 | 图层 React.memo | 已完成 | 4 个图层组件全部 memo 化 |
| 7 | CONTRIBUTING.md | 已完成 | 数据规范、构建门禁、图层开发规范、提交规范 |

#### v0.3 — 文档整理与规范化

| 方向 | 状态 | 说明 |
|------|------|------|
| PRD 与技术架构文档同步 | 已完成 | 对齐 /favorites、/search、收藏、对比、打印、PWA、LazyInteractiveMap、搜索页、懒加载 |
| docs 目录索引 | 已完成 | docs/README.md 新增产品 / 内容 / 模板三分法 + 使用向导 |
| 部署与 CI 文档 | 已完成 | DEPLOYMENT.md：12 步流水线、Vite base、SPA 回退、PWA、Netlify/Vercel/Nginx |
| 数据字典 | 已完成 | DATA_DICTIONARY.md：TS 类型字段速查、枚举、必填、校验、反糟粕门禁 |
| 更新日志 | 已完成 | CHANGELOG.md：v0.1 / v0.2 / v0.3 里程碑记录 |
| README 重写 | 已完成 | 徽章 / 核心特性 / 技术栈 / 命令 / 项目树 / 路由 / 门禁 / 索引 |
| 移除 IDE 本地文件夹跟踪 | 已完成 | .trae/ 与 .trae-html-share-packages/ 加入 .gitignore |

#### v0.3.1 — verifiedAt 180 天阻断前置软提醒机制（2026-08-24）

> 解决 v0.2 已经引入的 180 天阻断门禁「到日子才发现、立即卡死 CI」的运维风险。

| 方向 | 状态 | 说明 |
|------|------|------|
| 软提醒常量与纯函数 | 已完成 | `VERIFICATION_REMINDER_DAYS = 170` + `daysUntilStale / isInReminderWindow`，和 180 天阻断共享时间基准；11 条单元测试覆盖 9/10/11/169/170/171/179/180/181 天等关键边界 |
| 提醒脚本 | 已完成 | `scripts/check-verification-reminder.ts` 扫描 5 类数据；支持 human/JSON、exit-zero/exit-code；GitHub Actions 下输出 `::warning` 注解 |
| 脚本单元测试 | 已完成 | `src/test/check-verification-reminder.test.ts`，纯函数 + CLI 子进程，8 条新增用例全部通过 |
| 新命令 | 已完成 | `npm run validate:data:reminder`（本地排查）；`npm run validate:data:reminder:ci`（CI 读 JSON） |
| 构建期 warning-only 步骤 | 已完成 | `.github/workflows/deploy.yml` build job Step 5.1 插入；不阻断；仅打黄条 |
| 每周一自动工单 | 已完成 | `.github/workflows/verification-reminder.yml`（09:00 CST 周一）；标题日期去重，open 时只 comment 不新建 Issue |
| 文档同步 | 已完成 | DEPLOYMENT.md + DATA_DICTIONARY.md + CONTRIBUTING.md 三处写入；CHANGELOG 新增 v0.3.1 小节 |

#### v0.3.2 — 视觉、文档与发布收口（2026-08-25）

| 方向 | 状态 | 说明 |
|------|------|------|
| 全站视觉统一 | 已完成 | 统一沙金 / 胡杨绿 / 枸杞红色彩、卡片、徽标、页脚、响应式与焦点状态 |
| 网络实拍图片替换 | 已完成 | 景点、路线、美食、指南、手记、搜索、关于与 404 页面统一使用可追溯照片 |
| 图片边界与来源 | 已完成 | `IMAGE_PROVENANCE.md` 记录作者、许可、原始页面、响应式规格和参考范围规则 |
| 合并与文档验收 | 已完成 | 合并远端数据、文档与部署变更；补充 `RELEASE_STATUS.md` 作为发布验收入口 |
| 发布验证 | 已完成 | `main` 的 GitHub Actions build / deploy 均通过；Pages 首页已在线验收 |

#### v0.3.3 — 发布后交互细节（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 搜索框结构与聚焦态 | 已完成 | 使用明确的 `label` / `input` 关联，统一聚焦边框与提示阴影，窄屏结果摘要可换行 |
| 地图纯展示图层语义 | 已完成 | 政府标记与交通枢纽保留 `role="img"` 与 `aria-label`，移除无动作的 Tab 焦点 |
| 卡片与筛选聚焦态 | 已完成 | 景点、路线、专题卡片和列表搜索框统一悬停/键盘聚焦反馈，保持沙金焦点语言 |
| 发布回归 | 已完成 | 本地类型、静态、内容、单元、端到端、构建与 Lighthouse 全部通过；GitHub Pages 已在线 |

#### v0.3.4 — 地图图层语义与焦点细节收口（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 美食图层纯展示语义 | 已完成 | `FoodLayer` 在无 `onSelect` 时使用 `role="img"` 且不进入 Tab 顺序；交互模式保留按钮语义 |
| 美食卡片焦点反馈 | 已完成 | 键盘聚焦时与悬停状态同步图片缩放、饱和度和边框过渡 |
| 组件级回归 | 已完成 | 新增 `FoodLayer.test.tsx`，覆盖纯展示和键盘激活路径 |

#### v0.3.5 — 链接卡片键盘反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 链接卡片聚焦反馈 | 已完成 | 首页路线、行前指南天数卡、收藏列表和搜索结果统一悬停/键盘聚焦反馈 |
| 搜索结果可读区域 | 已完成 | 聚焦背景扩展到完整结果行，保留分组分隔层次 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.16 — 首页与搜索起始入口反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页探索入口 | 已完成 | “向下探索”和内容方法入口补齐悬停、键盘聚焦与轻量位移反馈 |
| 搜索起始入口 | 已完成 | 快捷关键词按钮统一边框、浅色背景、抬升与阴影反馈 |
| 回归覆盖 | 已完成 | 增加桌面与移动端首页/搜索入口状态回归，并通过全量检查 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.17 — 收藏操作反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 收藏按钮 | 已完成 | 景点与路线卡片、详情页和规划夹统一过渡、悬停、聚焦与轻量抬升反馈 |
| 状态语义 | 已完成 | 收藏前后保持朱砂红状态、边框层次和 `aria-pressed` 语义一致 |
| 回归覆盖 | 已完成 | 增加跨景点/路线页面收藏状态回归，并通过全量检查 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.18 — 景点对比操作反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 加入对比按钮 | 已完成 | 景点卡片统一过渡、悬停、键盘聚焦与轻量抬升反馈 |
| 对比状态 | 已完成 | 已加入对比与达到 3 个上限后的禁用状态保持清晰区分，并保留 `aria-pressed` 语义 |
| 回归覆盖 | 已完成 | 增加桌面端与移动端景点对比操作回归 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.19 — 旅行手记栏目切换反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 栏目标签 | 已完成 | 旅行手记四种内容类型统一悬停、键盘聚焦与轻量过渡反馈 |
| 选中语义 | 已完成 | 选中栏目保持胡杨绿高亮、清晰文字对比和 `aria-selected` 语义 |
| 回归覆盖 | 已完成 | 增加桌面端与移动端栏目切换状态回归 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.20 — 路线筛选入口反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 筛选按钮 | 已完成 | 路线天数、城市、主题和节奏筛选统一悬停、键盘聚焦与轻量抬升反馈 |
| 选中语义 | 已完成 | 选中筛选保持胡杨绿高亮、清晰文字对比和 `aria-pressed` 语义 |
| 回归覆盖 | 已完成 | 增加桌面端与移动端路线筛选状态回归 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.21 — 行前清单反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 清单条目 | 已完成 | 行前指南本机清单统一悬停、键盘聚焦与轻量抬升反馈 |
| 勾选状态 | 已完成 | 保持勾选后的胡杨绿背景、勾选图标与焦点轮廓清晰可辨 |
| 回归覆盖 | 已完成 | 增加桌面端与移动端清单条目反馈回归 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.22 — 搜索清空操作反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 清空按钮 | 已完成 | 搜索结果页的清空搜索按钮统一过渡、悬停、键盘聚焦与轻量抬升反馈 |
| 恢复行为 | 已完成 | 清空后保留搜索页入口并恢复输入框焦点，继续支持键盘连续操作 |
| 回归覆盖 | 已完成 | 增加桌面端与移动端有关键词搜索清空反馈回归 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.23 — 地图图层开关选中反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 选中状态 | 已完成 | 地图交通、美食、政府图层开关统一选中态胡杨绿、白字与阴影层次 |
| 悬停反馈 | 已完成 | 选中按钮悬停时保持状态样式，不再产生会干扰识别的抬升 |
| 回归覆盖 | 已完成 | 增加地图图层选中态桌面端与移动端端到端回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.24 — 导航当前页语义统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 搜索入口 | 已完成 | 桌面端搜索入口在当前页面补齐 `aria-current="page"` |
| 收藏入口 | 已完成 | 桌面端收藏入口在当前页面补齐 `aria-current="page"` |
| 回归覆盖 | 已完成 | 增加搜索与收藏当前页语义组件测试 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.25 — 景点兴趣筛选选中态统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 选中状态 | 已完成 | 景点兴趣筛选卡统一选中态胡杨绿边框、浅色底与内嵌阴影 |
| 悬停反馈 | 已完成 | 选中卡片悬停时保持固定层次，不覆盖当前筛选识别 |
| 回归覆盖 | 已完成 | 增加桌面端与移动端兴趣筛选选中态回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.29 — 移动端菜单入口反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 入口层次 | 已完成 | 移动端菜单按钮补齐胡杨绿、轻量抬升与阴影反馈，与搜索/收藏入口保持同一节奏 |
| 触控尺寸 | 已完成 | 保持 44px 触控尺寸，不改变现有菜单布局与焦点管理 |
| 回归覆盖 | 已完成 | 增加移动端菜单入口桌面端与移动端回归，桌面端自动跳过移动专属断言 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.30 — 安静文字操作热区稳定化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 反馈规则 | 已完成 | 清除筛选、清空筛选、清空对比、清空收藏和清单重置仅改变主题色，不整体位移 |
| 触控热区 | 已完成 | 保持 44px 触控高度，避免悬停时热区跟随文字移动 |
| 回归覆盖 | 已完成 | 增加景点、美食、手记和行前清单文字操作的跨页面端到端回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.31 — 详情页返回入口热区稳定化（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 返回入口 | 已完成 | 城市、景点、美食、路线与手记详情页返回链接整体热区保持稳定 |
| 方向反馈 | 已完成 | 仅移动左侧箭头，保留返回方向感与轻量过渡 |
| 回归覆盖 | 已完成 | 增加城市详情与路线详情桌面端、移动端返回入口回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.32 — 资料来源入口反馈统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 入口反馈 | 已完成 | 景点、美食、手记、指南、关于页与页脚外链统一末端图标轻量反馈 |
| 热区稳定 | 已完成 | 仅移动末端外链图标 2px，不整体移动来源链接热区 |
| 回归覆盖 | 已完成 | 增加景点、手记、指南和关于页来源入口桌面端、移动端回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.33 — 首页内容方法与搜索清空入口统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页内容方法入口 | 已完成 | 深色说明卡片中的文字入口补齐末端箭头反馈，不整体移动链接热区 |
| 搜索清空入口 | 已完成 | 清空按钮统一 44px 触控热区，仅改变背景、颜色与阴影，不整体位移 |
| 回归覆盖 | 已完成 | 增加首页内容方法入口与搜索清空入口桌面端、移动端回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.34 — 导航搜索入口触控尺寸统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 搜索入口尺寸 | 已完成 | 桌面端与移动端导航搜索入口统一为 44px 圆形热区 |
| 状态反馈 | 已完成 | 保留胡杨绿悬停/键盘聚焦反馈与 `aria-current` 当前页语义 |
| 回归覆盖 | 已完成 | 增加桌面端、移动端搜索入口尺寸回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.35 — 收藏入口触控尺寸统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 收藏入口尺寸 | 已完成 | 景点卡片、路线卡片、详情页与收藏夹收藏入口统一至少 44px 触控热区 |
| 状态反馈 | 已完成 | 保留收藏前后颜色、`aria-pressed` 语义和轻量抬升反馈 |
| 回归覆盖 | 已完成 | 增加景点与路线收藏入口桌面端、移动端尺寸回归 |
| 依赖安全 | 已完成 | 更新构建依赖锁定版本，npm audit 已恢复为 0 vulnerabilities |
| 发布回归 | 已完成 | GitHub Actions 校验与 Pages 部署成功，线上入口验收通过 |

#### v0.3.36 — 搜索快捷关键词触控尺寸统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 快捷关键词尺寸 | 已完成 | 搜索页快捷关键词统一至少 44px 触控热区 |
| 状态反馈 | 已完成 | 保留胡杨绿悬停、键盘聚焦和原有搜索跳转行为 |
| 回归覆盖 | 已完成 | 增加桌面端、移动端搜索快捷关键词尺寸回归 |
| 发布回归 | 已完成 | GitHub Actions 校验与 Pages 部署成功，线上入口验收通过 |

#### v0.3.37 — 次级操作入口触控尺寸统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 比较与目录入口 | 已完成 | 比较结果栏、正文目录与相关内容入口统一至少 44px 触控热区 |
| 目录解析稳定性 | 已完成 | 兼容正文 Windows 换行，确保目录锚点正常渲染与跳转 |
| 更新提示入口 | 已完成 | PWA 更新提示的刷新与关闭入口统一为 44px |
| 回归覆盖 | 已完成 | 增加比较结果栏与旅行手记目录桌面端、移动端回归 |
| 发布回归 | 已完成 | 主校验构建门禁、独立 Pages 构建部署与线上入口验收通过 |

#### v0.3.38 — 移动端收藏入口触控热区统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 收藏入口尺寸 | 已完成 | 移动端隐藏文字与数量后仍保持至少 44×44px 触控热区，与搜索和菜单入口对齐 |
| 状态反馈 | 已完成 | 保留朱砂红收藏状态、键盘聚焦、数量提示与 `aria-label` 语义 |
| 回归覆盖 | 已完成 | 增加桌面端、移动端导航收藏入口尺寸回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.39 — 品牌首页入口触控高度统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页入口尺寸 | 已完成 | 品牌首页入口保持至少 44px 触控高度，与搜索、收藏和菜单入口对齐 |
| 视觉比例 | 已完成 | 桌面与移动端 logo 尺寸继续按布局缩放，不改变既有品牌比例 |
| 回归覆盖 | 已完成 | 增加桌面端、移动端品牌首页入口尺寸回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.40 — 结果区筛选操作触控规范统一（2026-09-02）

| 方向 | 状态 | 说明 |
|------|------|------|
| 清除筛选尺寸 | 已完成 | 景点、路线与美食结果区“清除筛选”统一至少 44px 触控高度 |
| 视觉层级 | 已完成 | 保留低强调文字操作的颜色反馈，不改变筛选逻辑与 URL 同步 |
| 回归覆盖 | 已完成 | 增加桌面端、移动端结果区清除筛选入口尺寸回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.28 — 文字链接反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 文字入口反馈 | 已完成 | 全站文字链接补齐悬停与键盘聚焦时末端箭头 2px 轻量横向位移，与文字操作保持同一节奏 |
| 阅读层级 | 已完成 | 不增加背景和阴影，保持正文阅读层级与低占位入口定位；整条链接热区保持稳定 |
| 回归覆盖 | 已完成 | 增加首页路线入口与路线详情地图入口桌面端、移动端反馈回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.27 — 安静按钮层次反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 浅色页面层次 | 已完成 | 安静按钮补齐默认阴影，与主次行动按钮保持统一的轻量层次 |
| 深色头图适配 | 已完成 | 路线详情与行前指南的安静按钮使用深色环境阴影，避免浅色阴影污染头图层次 |
| 回归覆盖 | 已完成 | 增加 404 页面安静入口桌面端与移动端反馈回归，并复核深色头图按钮 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.26 — 次级行动按钮反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 默认层次 | 已完成 | 沙金色次级行动按钮补齐默认阴影，与主按钮形成清晰层级 |
| 悬停反馈 | 已完成 | 悬停与键盘聚焦统一换色、轻量抬升和阴影过渡 |
| 回归覆盖 | 已完成 | 增加首页旅行专题入口桌面端与移动端反馈回归 |
| 发布回归 | 已完成 | GitHub Actions 校验、Pages 构建与线上入口验收通过 |

#### v0.3.15 — 轻量文字操作状态统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 地图与筛选操作 | 已完成 | 地图层级返回、清除筛选和清空筛选统一颜色、位移与焦点反馈 |
| 本机清单操作 | 已完成 | 行前清单重置按钮在可用状态下与全站次级文字操作保持一致 |
| 回归覆盖 | 已完成 | 增加桌面与移动端文字操作状态回归，并通过全量检查 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.14 — 深色头图操作对比度收口（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 深色头图按钮 | 已完成 | 路线详情与行前指南的主、次按钮悬停和键盘聚焦状态保持清晰前景背景对比 |
| 回归覆盖 | 已完成 | 增加深色头图按钮高对比度端到端回归，并通过全量检查 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.6 — 导航焦点语言统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 顶部导航聚焦态 | 已完成 | 桌面导航、移动导航、搜索入口和收藏入口统一悬停/键盘聚焦反馈 |
| 移动菜单按钮 | 已完成 | 菜单按钮聚焦时同步边框、底色与主题色变化，并保留全局焦点轮廓 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.7 — 按钮与筛选控件焦点态统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 筛选控件聚焦态 | 已完成 | 景点兴趣、路线筛选和手记栏目切换统一悬停/键盘聚焦反馈 |
| 操作按钮聚焦态 | 已完成 | 收藏、对比、搜索建议、地图开关和更新提示按钮统一主题色反馈 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.8 — 导航与选择控件状态统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页天数选择 | 已完成 | 单选组支持鼠标悬停与键盘聚焦反馈，选中项保留清晰的高亮与焦点层次 |
| 路线按天跳转 | 已完成 | 粘性导航链接补齐悬停、聚焦和当前天状态，移动端横向滚动保持可读性 |
| 详情图片切换 | 已完成 | 图片切换点增加可见点击区域、悬停放大和键盘聚焦反馈 |
| 收藏与页脚操作 | 已完成 | 文字操作按钮和页脚链接补齐统一过渡与聚焦反馈 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.9 — 筛选面板反馈收口（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 筛选标签与下拉框 | 已完成 | 景点、美食、路线与旅行手记筛选控件统一容器聚焦、边框和提示阴影 |
| 移动端筛选入口 | 已完成 | “打开筛选”和结果快捷入口补齐悬停/键盘聚焦反馈，保持 44px 以上触控区域 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.10 — 次级目的地入口反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 五城概览卡片 | 已完成 | 卡片获得统一的抬升、边框、阴影和图片聚焦反馈 |
| 周边目的地与路线小卡 | 已完成 | 城市详情周边入口、路线小卡片补齐悬停/键盘聚焦状态 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.11 — 阅读入口状态统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 资料与正文入口 | 已完成 | 来源列表、正文目录、相关内容和行前资料链接补齐统一悬停/键盘聚焦反馈 |
| 路线与地图入口 | 已完成 | 路线停靠点、比较表名称链接、地图预览关闭按钮统一交互状态 |
| 首页专题卡片 | 已完成 | 专题卡片增加与景点、路线卡片一致的抬升、图片反馈和聚焦状态 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.12 — 首页与说明页入口收口（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 首页专题标题 | 已完成 | 标题链接在悬停与键盘聚焦时使用统一胡杨绿反馈 |
| 说明页资料目录 | 已完成 | 关于本站的外部资料入口补齐背景、颜色与键盘聚焦状态 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

#### v0.3.13 — 详情页操作反馈统一（2026-09-01）

| 方向 | 状态 | 说明 |
|------|------|------|
| 返回与图标操作 | 已完成 | 详情页返回链接、分享与关闭按钮统一悬停、聚焦和轻量动效 |
| 图片来源链接 | 已完成 | 图片说明中的来源入口补齐背景、颜色和键盘聚焦反馈 |
| 发布回归 | 已完成 | 单元、端到端、构建与 Lighthouse 通过 |

---

## v0.3.1+ 后续开发目标

### 阶段一：内容补全（优先级：高）

> 当前最大短板：亲历游记与探店手记为 0 篇，资料专题仅覆盖部分城市。

| 目标 | 当前状态 | 目标状态 | 涉及文件 |
|------|----------|----------|----------|
| 亲历游记 | 0 篇 published（3 篇 draft 模板） | ≥ 5 篇，覆盖银川、中卫、固原 | `src/content/journal/*.md` |
| 探店手记 | 0 篇 published（1 篇 draft 模板） | ≥ 3 篇，覆盖手抓羊肉、八宝茶、辣糊糊 | `src/content/journal/*.md` |
| 资料专题补全 | 15 篇 | 已达到阶段目标，继续补充真实亲历内容 | `src/content/journal/*.md` |
| 景点核实升级 | 2 个 review（南关清真大寺、中华黄河楼） | 补全直接来源后升 verified | `src/data/attractions.ts` |
| 美食核实升级 | 12 道 review | 逐步补全非遗/官方专页来源后升 verified | `src/data/foods.ts` |
| 草稿景点转正 | 1 个 draft（中华回乡文化园） | 补全来源后转 published | `src/data/attractions.ts` |

### 阶段二：功能增强（优先级：中）

| 目标 | 说明 | 依赖 |
|------|------|------|
| 路线打印优化 | 已完成：黑白高对比行程单，保留每日安排与核实信息，隐藏屏幕交互 | 无 |
| 收藏夹功能 | 已完成：基于 localStorage 的景点/路线收藏，导航显示数量，新增我的收藏页 | 无 |
| 景点对比 | 已完成：最多选择 3 个景点，横向对比类型、门票、时长、季节、预约、交通与核实状态 | 无 |
| 地图移动端优化 | 已完成：底部预览限制视口高度，支持安全区、Escape 和自动聚焦关闭 | 无 |
| 搜索增强 | 已完成：`/search` 统一搜索景点、美食、路线、城市和旅行专题，支持移动端入口 | 无 |
| 图片批量处理脚本 | 已完成：保留单文件模式，新增递归批量生成 WebP/AVIF 多尺寸变体 | 无 |

### 阶段三：性能与体验（优先级：中低）

| 目标 | 说明 | 当前指标 |
|------|------|----------|
| Lighthouse 性能提升 | 移动端 performance ≥ 0.95（当前门禁 0.9） | 4 个 URL 审计 |
| 图片懒加载 | 已完成：ResponsiveImage 默认 lazy + async，景点/城市/专题详情主图显式 eager + high | 已覆盖图片组件与主要详情页 |
| 手记正文代码高亮 | react-markdown 暂不支持代码块高亮 | 可引入 rehype-highlight |
| 地图首屏加载 | 已完成：首页地图组件与省级边界在接近视口时加载，区县数据仍按城市点击后加载 | IntersectionObserver + 320px 预加载距离 |
| 地图边界简化 | 已完成：移动端使用 0.003° 容差的省级简化边界，桌面端保留原始细节 | `ningxia-province-mobile.json`，4,891 → 2,121 个坐标点（约 50KB） |
| PWA 离线支持 | 已完成：生产环境注册 Service Worker，缓存静态资源、地图数据和已访问页面，离线时回退首页 | `manifest.webmanifest` + `sw.js` |

### 阶段四：工程化（优先级：低）

| 目标 | 说明 | 当前状态 |
|------|------|----------|
| CI 缓存优化 | GitHub Actions 缓存 node_modules 与 Vite 构建产物；Playwright 浏览器已有 cache v5 | node_modules 缓存通过 setup-node cache 实现；Vite 产物待验证 |
| verifiedAt 180 天前置软提醒 + 每周一自动工单 | 防止 verifiedAt 集中过期时突然卡死：170 天阈值脚本 + deploy 构建 warning-only 步骤 + 每周一 schedule Issue | **已完成**；见 v0.3.1 |
| 依赖版本升级 | React 19 / Vite 7 评估 | 当前 React 18 / Vite 6 |
| 视觉回归测试 | 引入 Playwright 截图对比，防止 UI 意外变更 | 仅功能 E2E |
| 国际化（i18n） | 评估多语言需求（英文版面向国际游客） | 当前仅中文 |
| 数据 API 化 | 评估将 TS 数据模块迁移为 CMS/API 的必要性 | 当前静态 TS 模块 |

---

## 技术债务清单

| 编号 | 描述 | 影响 | 处理方式 |
|------|------|------|----------|
| TD-02 | 图片处理脚本缺少统一的源图批量入口 | 已解决 | `npm run process:images -- --batch <源图目录> <输出目录>` |
| TD-03 | E2E 测试依赖 Playwright 浏览器下载 | CI 环境可能受限 | 已有回退策略（browser 子代理） |
| TD-04 | .trae/ 仍保留在本地工作区 | 不影响 git，但占磁盘 | 属 IDE 行为，无需处理 |

---

## 版本节奏

| 版本 | 主题 | 状态 |
|------|------|------|
| v0.1 | 基础框架与核心功能（地图 / 景点 / 城市 / 路线 / 手记） | 已完成 |
| v0.2 | 7 轮持续优化（美食 / 时间槽 / 图层 a11y / 校验门禁 / memo / 贡献指南） | 已完成 |
| v0.3 | 文档整理与工程规范化（README 重写、DEPLOYMENT、DATA_DICTIONARY、CHANGELOG、docs 索引） | 已完成 |
| v0.4 | 内容补全（亲历游记/探店 ≥ 8 篇，草稿景点 / review 内容升级） | 进行中 |
| v0.5 | 功能增强打磨（打印样式完善 / 视觉回归 / 移动端细节） | 待启动 |
| v0.6 | 性能与工程化（CI 缓存、依赖升级评估、i18n/CMS 评估） | 待启动 |

---

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-08-17 | v0.3 | 初始创建；归档 v0.1—v0.3 已完成目标；规划 v0.4—v0.6 后续方向 |
| 2026-08-17 | v0.3 | 首次正式发布至 main；与 docs/product/ 目录规范对齐 |
| 2026-08-18 | v0.3 | 补齐 DEPLOYMENT / DATA_DICTIONARY / docs/README / CHANGELOG；PRD 与技术架构文档同步当前实现 |
| 2026-08-24 | v0.3.1 | 落地 verifiedAt 170–180 天软提醒机制：脚本、构建 Step 5.1、每周一 schedule Issue、命令、单测、三处文档 |

