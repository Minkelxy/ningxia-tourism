# 文档索引

> v0.3.135 更新：全站长页面滚动时沿顶部实时绘制阅读进度墨线，并以轻量笔头提示当前位置；保留页面内容、滚动回顶、锚点跳转与打印版布局，减少动效设置下即时更新；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.133 更新：移动端导航菜单与页脚当前入口增加统一激活墨线，与桌面主导航共享纸面状态反馈语言；保留导航路径、当前页语义、菜单焦点与滚动行为，减少动效设置下恢复静态显示；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.132 更新：全站收藏按钮切换为已收藏时增加轻量心形落印与底部手绘墨线，与筛选、栏目和路线日程共享纸面状态反馈语言；保留收藏逻辑、跨页面同步和窄屏图标化布局，减少动效设置下恢复静态显示；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.129 更新：旅行手记当前栏目标签增加沿底部展开的激活墨线，与全站导航、路线日程和标题墨线共享纸面反馈语言；保留栏目切换、键盘操作和窄屏横向浏览，减少动效设置下恢复静态显示；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.128 更新：地图 SVG 底图增加低对比度等高线墨线，按地形簇错峰落笔并随地图视口自然移动；属于非交互装饰，减少动效设置下恢复静态显示；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.127 更新：交互地图增加固定在画布角落的北向罗盘纸面绘图，首次进入轻量落印且不随拖拽/缩放移动；提供明确的无障碍名称，减少动效设置下恢复静态显示；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.126 更新：路线详情按天快速跳转的激活日增加沿底边落笔墨线，并按天数索引错峰入场；保留锚点跳转、滚动激活、键盘访问与窄屏横向浏览，减少动效设置下恢复静态激活墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.125 更新：交通、美食和政府地图标记开启时共享轻量墨线环落印；保留图层开关、点位语义、键盘/触控行为与地图层级切换，减少动效设置下恢复静态圆环；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.124 更新：路由进入不同页面时，主内容共享统一的轻量入场过渡；保留滚动回顶、主要内容焦点、移动端菜单、筛选与深链接行为，减少动效设置下恢复静态页面；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.123 更新：地图进入城市或区县视图时，行政区、标签、景点及已开启图层按当前范围重新按阅读顺序渐进落印；保留缩放、拖拽、键盘进入、点位选择、预览、图层开关和图例联动，减少动效设置下恢复静态图层；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.122 更新：景点与美食关键词、城市或类型筛选变化时，结果摘要、卡片网格和空结果重新按阅读顺序渐进绘图；保留筛选、清除、收藏、对比与详情链接，减少动效设置下恢复静态结果；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.121 更新：全站搜索关键词变化时，结果摘要、分类结果组、结果条目和无结果状态重新按阅读顺序渐进绘图；保留关键词提交、建议词、清空搜索、URL 同步和结果链接，减少动效设置下恢复静态结果；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.120 更新：旅行手记切换栏目、城市、标签或搜索词时，结果摘要、文章卡片和空结果重新按阅读顺序渐进绘图；保留 URL 同步、栏目键盘切换、清空筛选与文章链接，减少动效设置下恢复静态结果；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.119 更新：路线筛选组合变化时，结果摘要、横向比较表和路线卡片重新按阅读顺序渐进绘图；保留 URL 同步、筛选语义、比较表、收藏和路线链接，减少动效设置下恢复静态结果；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.118 更新：地图景点预览面板的图片、位置眉题、标题墨线、摘要和行动入口按阅读顺序分层渐进绘图，切换景点时重新铺开面板；保留关闭、Esc 返回焦点、详情链接和移动端底部面板行为，减少动效设置下恢复静态内容与标题墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.117 更新：地图区县图例面板、标题、条目与色块按阅读顺序错峰渐进绘图，切换城市时重新铺开图例；保留图例键盘聚焦、区域高亮和地图层级行为，减少动效设置下恢复静态图例；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.116 更新：地图工具栏、层级面包屑、当前层级标签和控制按钮按阅读顺序渐进绘图，图层启用增加轻量落印提示；保留地图缩放、重置、层级切换、图层与键盘行为，减少动效设置下恢复静态工具栏与墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.115 更新：全站固定头部补充整体入场、品牌标识落印和桌面当前导航墨线；保留导航、收藏、搜索、移动端菜单与焦点行为，减少动效设置下恢复静态头部与当前导航墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.114 更新：首页路线查找、专题卡片、行前指南入口、旅行手记入口和资料提示按区块、标题墨线与内容顺序渐进绘图；保留筛选、链接与资料说明行为，减少动效设置下恢复静态内容与墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.113 更新：空结果、404/未找到、加载、分享、更新和离线提示统一补充状态容器、图标、标题墨线、说明与操作的轻量渐进绘图；保留原有状态语义与操作行为，减少动效设置下恢复静态内容与墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.112 更新：景点主题、筛选面板、比较区域、结果表格、路线卡片以及关于本站方法卡片统一按阅读顺序渐进绘图；保留筛选、收藏、对比和链接行为，减少动效设置下恢复静态；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.111 更新：收藏页与全站搜索页补充首屏、标题墨线、工具栏/表单、结果分组和内容行的渐进入场，保留收藏与搜索逻辑，减少动效设置下恢复静态；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.110 更新：路线详情首屏、事实信息、日程导航、DAY 标记与资料侧栏按阅读顺序分层入场，DAY 标记补充统一手绘环线；不改变路线跳转、打印、分享、收藏和时间线逻辑，减少动效设置下恢复静态；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.109 更新：景点、美食与城市详情补充首图、标题墨线、信息区块、资料侧栏和关联路线的分层入场；景点/美食/五城列表卡片按阅读顺序错峰出现，不改变内容与链接逻辑，减少动效设置下恢复静态；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.108 更新：旅行手记列表按阅读顺序错峰入场并展开标题墨线，文章详情按封面、标题、事实摘要、正文与资料侧栏分层入场；不改变内容与链接逻辑，减少动效设置下恢复静态阅读层级与墨线；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.107 更新：行前指南首屏、四季/天数卡片和交通流程补充错峰入场与方向性绘图，清单勾选增加轻量落笔反馈；不改变行前信息、链接和清单逻辑并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.106 更新：首页按天数筛选路线结果增加错峰入场与标题展开墨线，与地图和路线页保持同一轻量绘图语言；不改变筛选交互并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.105 更新：选中景点增加独立 SVG 手绘环线，与区域聚焦和路线节点保持同一反馈语言；不改变点位交互并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.104 更新：路线详情时间线的停靠点增加错峰落印手绘环线，时段细节补齐非编号节点，与地图区域墨线使用同一轻量绘图语言；装饰不改变路线语义并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.103 更新：地图区域在键盘聚焦、区县图例联动和选中区县时增加沿边界展开的轻量高亮墨线，保留交互语义并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.102 更新：首页山河绘图增加远景山体、近景山体、印章与探索提示的分层入场节奏，保留响应式构图并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.101 更新：地图标签、景点、美食、政府和交通点位统一增加轻量弹入绘图，保留定位与操作逻辑，并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.100 更新：路线详情时间线增加渐进绘图轨道与停靠点错峰入场，窄屏位置保持一致，并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。
> v0.3.99 更新：地图缩放、重置和层级切换增加平滑视口过渡，拖拽时保持即时跟手，并尊重减少动效设置；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.98 更新：首页 CSS 山河绘图与地图 SVG 区域增加轻量入场动效，并在减少动效设置下恢复静态绘图；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.97 更新：景点与美食筛选面板的搜索输入本体补齐为至少 44px 高，与下拉控件和主要操作共享触控基线；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.96 更新：首页、全屏状态页和离线提示统一引用响应式站点头部高度变量；文档索引中的当前版本描述同步修正，完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.95 更新：全站搜索输入控件本体统一为至少 44px 高，与筛选输入和其他主要操作共享触控基线；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.94 更新：320px—480px 地图控制区统一为三列两行，地图图层与缩放操作在不同手机宽度下保持同一排列节奏；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.93 更新：收藏列表在 320px—480px 下统一收束标题/副标题与路线摘要，320px—360px 使用图标化收藏操作，保持不同移动宽度下的行高节奏；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.89 更新：行前指南首屏在 320px、360px 下将两个主要入口收口为等宽同排；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.88 更新：路线详情在 320px、360px 极窄屏下将打印、分享、收藏入口保持同一行；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.87 更新：路线详情在 320px、360px 极窄屏下将打印、分享、收藏入口收口到同一行；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.86 更新：详情关联卡片与搜索结果末端箭头统一使用胡杨绿和轻量右移反馈；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.85 更新：城市详情“精选目的地”卡片在移动端同步图片与内容列宽，避免窄屏裁切；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.84 更新：五城概览卡片与全站内容卡片统一采用“内容填充、行动收口”的弹性布局；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.83 更新：五城概览卡片与全站内容卡片统一采用“内容填充、行动收口”的弹性布局；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.82 更新：旅行手记列表卡片与全站内容卡片统一采用“内容填充、行动收口”的弹性布局；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.81 更新：首页旅行专题卡片与景点、美食、推荐路线卡片统一采用“内容填充、行动收口”的弹性布局；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.80 更新：景点、美食与推荐路线卡片统一采用“内容填充、行动收口”的弹性布局；完整验收记录见 `RELEASE_STATUS.md`。

> v0.3.79 更新：推荐路线卡片在桌面双列中保持行动入口底部对齐；完整验收记录见 `RELEASE_STATUS.md`。

`docs/` 目录收录项目的**发布、产品、技术、内容与贡献**相关文档，按主题分为三个子目录；当前发布快照与验收记录位于根部的 `RELEASE_STATUS.md`。

- 当前实现快照已更新至 v0.3.135：全站长页面滚动时沿顶部实时绘制阅读进度墨线，并与全站导航、收藏、筛选、手记栏目、地图绘图、路线按天导航和当前 DAY 印章共享轻量动效基线；装饰不参与交互，保留页面内容、滚动回顶、锚点跳转、滚动识别、键盘操作、打印版布局、导航路径、收藏、筛选、URL 同步和窄屏布局，并在减少动效设置下即时更新。

- 当前交互与发布复核版本为 v0.3.127；固定头部、页面切换、地图工具栏、地图区县图例、景点预览面板、首页山河绘图、地图方向罗盘、地图区域入场、地图视口过渡、地图叠加图层墨线落印、路线时间线绘图、路线按天导航激活墨线、地图层级图层重绘、各类筛选结果重绘、全站搜索结果重绘和区域聚焦墨线共享轻量动效基线，品牌标识、当前导航、页面主内容、地图层级、图例、预览内容、方向罗盘和筛选结果按层级依次落印，罗盘固定在画布角落且不随拖拽/缩放移动，区域聚焦沿边界展开，路线激活日沿底边落笔，并在减少动效设置下恢复静态绘图；列表筛选输入、全站搜索和主要操作在桌面/移动端保持至少 44px 高；收藏列表、地图控制区、景点卡片、路线详情和行前指南继续共享统一的响应式操作节奏，并由双端 E2E、生产构建、Lighthouse 与 Pages 回退校验共同覆盖。

```
docs/
├── README.md                 ← 本文档：目录与使用指引
├── RELEASE_STATUS.md         ← 当前数据规模、验收命令与图片边界
├── product/                  产品与工程（工程向必读）
│   ├── 宁夏旅游地图PRD.md          产品需求文档：用户、功能、UI、内容全景
│   ├── 宁夏旅游地图技术架构.md     技术架构文档：分层、组件、路由、数据模型、测试、部署
│   ├── DEVELOPMENT_PLAN.md        开发里程碑、已完成阶段、后续规划与技术债务
│   ├── DEPLOYMENT.md              部署与 CI/CD：GitHub Actions 流水线与本地部署指南
│   ├── DATA_DICTIONARY.md         数据字典：TS 数据模块全字段速查表
│   └── optimization-7rounds-plan.md  历史 7 轮优化计划（已归档，供溯源）
│
├── content/                  内容与资产（内容维护向必读）
│   ├── CONTENT_AUDIT.md           内容审计：景点 / 美食 / 枢纽 / 手记的分级与复核记录
│   ├── IMAGE_PROVENANCE.md        图片来源：实拍/官方图片、许可与处理记录
│   ├── MAINTENANCE.md             维护手册：日常新增 / 复核 / 发布操作准则
│   └── attraction-template.json   新增景点数据 JSON 模板（参考字段清单）
│
└── templates/                内容模板（不参与站点构建）
    ├── editorial-topic.md         资料型旅行专题（editorial + type: guide）
    ├── travel-journal.md          亲历游记（firsthand + type: travel）
    ├── food-journal.md            探店手记（firsthand + type: food）
    └── route-template.md          路线规划模板（供数据录入参考）
```

## 如何使用文档

| 你想做什么 | 应该先看 |
|-----------|---------|
| 快速跑起来项目 | 根目录 [README.md](../README.md) |
| 查看当前发布状态与验收结果 | [RELEASE_STATUS.md](RELEASE_STATUS.md) |
| 提交数据 / 代码 PR | 根目录 [CONTRIBUTING.md](../CONTRIBUTING.md) + [content/MAINTENANCE.md](content/MAINTENANCE.md) |
| 理解**产品为什么做这些功能** | [product/宁夏旅游地图PRD.md](product/宁夏旅游地图PRD.md) |
| 理解**代码怎么组织、技术选型原因** | [product/宁夏旅游地图技术架构.md](product/宁夏旅游地图技术架构.md) |
| 查看 / 添加一条数据，不清楚字段含义 | [product/DATA_DICTIONARY.md](product/DATA_DICTIONARY.md) |
| 部署到自己的 Pages / 静态托管 | [product/DEPLOYMENT.md](product/DEPLOYMENT.md) |
| 判断一条内容应该标 verified / review | [content/CONTENT_AUDIT.md](content/CONTENT_AUDIT.md) |
| 新增手记 / 探店 | [templates/](templates/) 下对应模板 + [content/MAINTENANCE.md](content/MAINTENANCE.md) |
| 查看版本变化与重要更新 | 根目录 [CHANGELOG.md](../CHANGELOG.md) |

## 文档维护原则

1. **单一事实来源（SSOT）**：
   - 字段级定义以 `src/types/index.ts` 为准；`DATA_DICTIONARY.md` 是其人类可读镜像，字段变更时同步更新。
   - 数据级记录以 `src/data/*.ts` 和 `src/content/journal/*.md` 为准；`CONTENT_AUDIT.md` 记录分级与复核历史。
2. **日期必须同步**：任何内容核实后，`verifiedAt`、`sources[].checkedAt`、`CONTENT_AUDIT.md` 顶部日期三处一起更新。
3. **归档不删除**：历史计划（如 `optimization-7rounds-plan.md`）保留作溯源，但会在标题或顶部标注「已归档」。
4. **跨文档引用**：README 中只给出链接，不重复粘贴正文内容。
