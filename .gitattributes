# 统一以 LF 存入仓库，并以 LF 检出。
#
# 本仓库历史上没有任何行尾约定，30 个文件是 CRLF、141 个是 LF，个别文件（如
# src/index.css、package.json）甚至单文件内混用。后果是：编辑器一旦改写某个文件，
# 就会把它整体翻成同一种行尾，于是一次 139 行的真实改动会裹上约 2000 行行尾噪音
# （v0.3.138 的 src/index.css 就是如此），提交变得无法 review。
#
# 加此文件后，git 在提交时把文本文件规范化为 LF、检出时也写 LF，行尾不再随编辑器漂移。
# 二进制文件由 text=auto 自动识别，不受影响。
* text=auto eol=lf
